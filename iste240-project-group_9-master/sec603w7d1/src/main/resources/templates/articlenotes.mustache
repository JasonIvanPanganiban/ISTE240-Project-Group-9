<!DOCTYPE html>
<html>
<head>
    <title>Article Notes</title>

    <!-- Internal CSS styles -->
    <style>
        /* Basic page styling */
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            padding: 0;
            background-color: #f5f5f5;
        }

        /* Main headings */
        h1 {
            color: #333;
            text-align: center;
        }

        /* Container box for main content */
        .container {
            max-width: 1000px;
            margin: 0 auto;
            background-color: white;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }

        /* Table styles */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        th, td {
            padding: 10px;
            border: 1px solid #ddd;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }

        /* Form styling */
        form {
            background-color: #f9f9f9;
            padding: 15px;
            border-radius: 5px;
            margin-top: 20px;
        }
        .form-group {
            margin-bottom: 15px;
        }

        /* Labels */
        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        /* Input and Textarea fields */
        input[type="text"],
        input[type="number"],
        textarea {
            width: 100%;
            padding: 8px;
            border: 1px solid #ddd;
            border-radius: 4px;
            box-sizing: border-box;
        }

        /* Textarea resizing */
        textarea {
            height: 100px;
            resize: vertical;
        }

        /* Submit button styling */
        button {
            background-color: #4CAF50;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        button:hover {
            background-color: #45a049;
        }

        /* Special styling for notes cell */
        .notes-cell {
            max-width: 400px;
            white-space: pre-wrap; /* Preserves line breaks */
        }

        /* Links area above the page */
        .links {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-bottom: 10px;
            flex-wrap: wrap;
        }

        /* Link button styles */
        .links a {
            background-color: #4CAF50;
            color: whitesmoke;
            padding: 5px 10px;
            border-radius: 10px;
            font-weight: bold;
            transition: all 0.3s ease;
        }

        /* Hover effect for links */
        .links a:hover {
            background-color: #27b161;
            color: #45a049;
        }
    </style>
</head>

<body>

<!-- Navigation links at the top -->
<div class="header-links">
    <div class="links">
        <a href="index.html"><div>Index</div></a>
        <a href="progress-dashboard.html"><div>Dashboard</div></a>
        <a href="learning-hub.html"><div>Learning Hub</div></a> <!-- Note: two Dashboard links -->
        <a href="quiz.html"><div>Quiz</div></a>
    </div>

    <!-- Main Page Title -->
    <h1>Green Planet: Teen Sustainability Guide</h1>
</div>

<!-- Main container for article notes -->
<div class="container">
    <h1>Article Notes</h1>

    <!-- Table displaying all article notes -->
    <table>
        <thead>
        <tr>
            <th>Index</th>
            <th>Article Name</th>
            <th>Notes</th>
        </tr>
        </thead>
        <tbody>
        {{#articlenotes}}
            <!-- Display each article note using Mustache templating -->
            <tr>
                <td>{{index}}</td>
                <td>{{articleName}}</td>
                <td class="notes-cell">{{notes}}</td>
            </tr>
        {{/articlenotes}}
        {{^articlenotes}}
            <!-- Message when no notes are available -->
            <tr>
                <td colspan="3">No article notes available</td>
            </tr>
        {{/articlenotes}}
        </tbody>
    </table>

    <!-- Form to add a new article note -->
    <h2>Add New Article Note</h2>
    <form method="POST" action="/articlenotes">
        <div class="form-group">
            <label for="index">Index:</label>
            <input type="number" id="index" name="index" required>
        </div>

        <div class="form-group">
            <label for="articleName">Article Name:</label>
            <input type="text" id="articleName" name="articleName" required>
        </div>

        <div class="form-group">
            <label for="notes">Notes:</label>
            <textarea id="notes" name="notes" placeholder="Enter a few notes from the article you've read"></textarea>
        </div>

        <!-- Submit button -->
        <button type="submit">Add Article Note</button>
    </form>
</div>

</body>
</html>
